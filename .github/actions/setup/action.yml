name: Set up Nix
description: >-
  Nixを固定した版で導入する。checkoutはこのactionを参照するために先に実行が必要なため、
  各jobが自分で行う。installerの版更新はこのファイルの1か所で済む。

runs:
  using: composite
  steps:
    - name: Install Nix
      uses: DeterminateSystems/determinate-nix-action@4eea0b33e3d1f02ecfe37cf16e7204c424009606 # v3.21.0
