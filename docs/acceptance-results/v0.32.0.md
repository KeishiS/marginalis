# v0.32.0受入結果

## 対象

- タグ: `v0.32.0`
- 対象: `codex/v0.32.0-release`のリリース候補
- 対象コミット: 未確定
- 判定日: 未実施
- 公開判定: 未判定

## 結果

| 区分 | 結果 | 実施日 | 証跡・判断 |
| --- | --- | --- | --- |
| 自動検証 | 未実施 | — | リリースPull Requestで、React・TypeScript、Rust、公開契約、依存境界、設計条件との対応、カバレッジ、ブラウザー試験、AArch64、NixOS VM E2Eを実行する |
| NixOS配備 | 未実施 | — | v0.31.0でarchive 15を書き出してarchive 16へ変換し、空のschema 20へ復元した後、起動、OIDCログイン、Web UI、設定、MCP再接続を確認する |
| archive移行・復元 | 未実施 | — | archive 15をarchive 16へ変換して隔離した空のschema 20へ取り込み、ノート、ACL、日時、revision、書誌情報、数式マクロ設定が一致することを確認する |
| 初回取込 | 未実施 | — | 新しい取込元とCSL-JSONファイルを事前確認し、新規項目を一括で保存できること、外部側の内容を展開できることを確認する |
| 再取込と競合解決 | 未実施 | — | 同じ取込元の再取込で、外部側更新、Marginalis側保持、双方変更、重複候補、取込不可を区別し、双方の内容を比較してキーボードだけで処理を選べることを確認する |
| 保存規則 | 未実施 | — | 外部側を採用してもcitation keyを維持し、入力から消した項目を削除せず、選択した計画全体を一つのtransactionで保存することを確認する |
| 事前確認の失効 | 未実施 | — | 事前確認後に別画面で書誌情報を更新すると適用全体が競合として拒否され、改めて事前確認できることを確認する |
| 取込元の復元 | 未実施 | — | archive 16の再書き出しと隔離復元で、取込元、外部項目との対応、前回取込時の基準が維持されることを確認する |

未実施の項目があるため、現時点では公開を判定しません。

## 今回の確認で重点を置く点

CSL-JSONの取り込みは外部文献管理ツールからMarginalisへの一方向操作です。外部サービスの認証情報や
元ファイルは保存せず、取込元の表示名、外部項目との対応、前回取込時の内容digestとrevisionだけを
保存します。DOIなどの一致は重複候補を示す根拠であり、自動統合の根拠にはしません。

事前確認はdatabaseを変更しません。適用時には、事前確認が基準にした書誌ライブラリーと取込元の状態を
同じtransaction内で再確認し、一つでも変わっていれば全体を保存しません。入力に含まれない書誌情報は
削除せず、既存項目へ外部側の内容を反映する場合もcitation keyを維持します。

v0.31.0のschema 19は直接使用せず、archive 15を書き出してarchive 16へ変換し、空のschema 20へ
取り込みます。v0.31.0には取込元と対応情報がないため、移行後に最初にCSL-JSONを取り込んだ時点から
記録します。MCPのclient登録とtokenは移行されないため、復元後に接続し直します。

利用者、Cookie、token、認可code、PKCE verifier、実際のノート本文や書誌情報は記録しません。
